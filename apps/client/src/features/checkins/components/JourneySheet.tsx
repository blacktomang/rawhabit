import type { CheckIn } from "@rawhabit/shared";

export function JourneySheet({ checkIns, onClose }: { checkIns: CheckIn[]; onClose: () => void }) {
  return <div className="sheet-backdrop" role="presentation" onMouseDown={onClose}><section className="journey-sheet" role="dialog" aria-modal="true" aria-label="Your private check-in journey" onMouseDown={(event) => event.stopPropagation()}><button className="sheet-close" onClick={onClose} aria-label="Close check-in journey">×</button><p className="eyebrow">Private journey</p><h2>Your honest days</h2><p className="journey-intro">Only you can see this record. Each day keeps its coach response and action.</p>{checkIns.length ? <ol className="journey-list">{checkIns.map((checkIn) => <li key={checkIn.id}><div className="journey-day"><span>Day {checkIn.day}</span><time dateTime={checkIn.challengeDate}>{new Date(`${checkIn.challengeDate}T00:00:00`).toLocaleDateString()}</time></div><p>{checkIn.caption}</p><small>Coach: {checkIn.coach.coachMessage}</small>{checkIn.coachPlan && <small>Reflect: {checkIn.coachPlan.socraticPrompt}</small>}</li>)}</ol> : <p className="empty-state">Your private check-ins will build a record here, one honest day at a time.</p>}</section></div>;
}
