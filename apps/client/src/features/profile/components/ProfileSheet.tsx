import type { EarnedBadge } from "@rawhabit/shared";

export function ProfileSheet({ displayName, badges, onClose }: { displayName: string; badges: EarnedBadge[]; onClose: () => void }) {
  return <div className="sheet-backdrop" role="presentation" onMouseDown={onClose}><section className="profile-sheet" role="dialog" aria-modal="true" aria-label={`${displayName}'s profile`} onMouseDown={(event) => event.stopPropagation()}><button className="sheet-close" onClick={onClose} aria-label="Close profile">×</button><span className="avatar profile-avatar">{displayName.slice(0, 1)}</span><p className="eyebrow">RawHabit profile</p><h2>{displayName}</h2><p className="profile-copy">Badges are earned by finishing a challenge. Hover or focus one to see the challenge behind it.</p><div className="badge-grid">{badges.length ? badges.map((badge) => <span className={`earned-badge ${badge.kind}`} key={badge.id} tabIndex={0}><b>{badge.kind === "habit_builder" ? "Habit Builder" : "Pattern Breaker"}</b><span className="badge-detail" role="tooltip">{badge.challengeTitle}<br />Completed {new Date(badge.earnedAt).toLocaleDateString()}</span></span>) : <p className="empty-state">No badges yet. Complete a challenge to earn your first one.</p>}</div></section></div>;
}
